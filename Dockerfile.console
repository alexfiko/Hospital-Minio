FROM quay.io/minio/minio

# Esto imprimirá en los logs el comando que estás usando
CMD ["/bin/sh", "-c", "echo '🔍 Ejecutando consola en puerto 80...' && minio server /data --console-address :80"]
